name: task_manager
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  go_router: ^12.1.1
  equatable: ^2.0.5
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_flavorizr: ^2.2.1

flutter:
  uses-material-design: true

  assets:
    - assests/images/

# 👇 THIS MUST BE OUTSIDE dev_dependencies
flavorizr:
  app:
    android:
      flavorDimensions: "flavor-type"

  instructions:
    - assets:download
    - assets:extract
    - android:androidManifest
    - android:flavorizrGradle
    - android:buildGradle
    - android:dummyAssets
    - android:icons
    - flutter:flavors
    - flutter:app
    - flutter:pages
    - flutter:main
    - assets:clean
    - ide:config

  flavors:
    dev:
      app:
        name: "Task Manager Dev"
      android:
        applicationId: "com.spowerz.taskmanager.dev"
        applicationIdSuffix: ".dev"

    prod:
      app:
        name: "Task Manager"
      android:
        applicationId: "com.spowerz.taskmanager"
